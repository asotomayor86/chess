; @neondatabase/auth declara un peerOptional next>=16 (para su SDK de servidor y
; sus componentes de UI, que aquí NO usamos: solo el cliente createAuthClient, que
; es un wrapper de fetch). El juego va en Next 14, así que aceptamos ese peer opcional.
legacy-peer-deps=true
